MacOS-Launchpad-Resizer

The MacOS Launchpad is great, but we can't resize it like we can the OS X Dock. I have created a simple script to change the layout of the Launchpad to the desired size. The default is 7x5, I now used 10x10.
